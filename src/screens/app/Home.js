// A tela Home foi substituída pela tela Dashboard Interativo.
// Este arquivo é mantido apenas como placeholder para evitar erros de importação imediata, mas será ignorado.
import React from 'react';
import { View, Text } from 'react-native';
const HomeScreen = () => (<View><Text>Substituído pelo Dashboard</Text></View>);
export default HomeScreen;
