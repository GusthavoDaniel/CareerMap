// A tela Profile foi substituída pela tela Achievements (que contém o perfil).
// Este arquivo é mantido apenas como placeholder para evitar erros de importação imediata, mas será ignorado.
import React from 'react';
import { View, Text } from 'react-native';
const ProfileScreen = () => (<View><Text>Substituído pelo Achievements</Text></View>);
export default ProfileScreen;
